{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('fos_user_registration_register') }}">Create new user</a>
<ul>
    {% for user in users %}
        <li>
            <a href="{{ path('user_edit', {'id':user.id}) }}">{{ user.email }}</a>
        </li>
    {% endfor %}
</ul>



{% endblock %}