{% extends 'base.html.twig' %}

{% block body %}
<ul>
    {% for user in users %}
        <li>
            {{ user.email }}
        </li>
    {% endfor %}
    {% for role in users %}
        <li>
            {{ role }}
        </li>
    {% endfor %}
</ul>
    <a href="{{ path('fos_user_registration_register') }}">Create new user</a>
{% endblock %}