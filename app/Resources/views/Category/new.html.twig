{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nauja kategorija</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input style="margin-bottom: 20px; margin-top: 20px;" class="btn btn-success" type="submit" value="Sukurti" />
    {{ form_end(form) }}

    <a class="btn btn-primary" href="{{ path('faq_index') }}">Atgal į sąrašą</a>
{% endblock %}
