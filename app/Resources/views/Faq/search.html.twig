{% extends 'base.html.twig' %}

{% block body %}
    {{ dump(request) }}
{{ dump(result) }}
    {{ dump(form) }}
   {# {% for question in result %}
        {{ question.question }}
    {% endfor %}#}
{% endblock %}