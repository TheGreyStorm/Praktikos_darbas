{% extends 'base.html.twig' %}

{% block body %}
    <h1>Naujas klausimas</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Sukurti" class="btn btn-success" style="margin-top: 10px" />
    {{ form_end(form) }}

            <a class="btn btn-primary"  style="margin-top: 10px" href="{{ path('faq_index') }}">Atgal į sąrašą</a>

{% endblock %}
